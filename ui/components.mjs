export const escape = (value = '') => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paths = { home:'M3 10 12 3l9 7v11h-6v-7H9v7H3Z', planner:'M4 5h16v16H4ZM8 2v6m8-6v6M4 10h16', todo:'M4 4h16v16H4Zm4 8 3 3 6-7', notes:'M5 3h11l3 3v15H5Zm4 5h6m-6 4h6m-6 4h6', folder:'M3 6h7l2 2h9v12H3Z', search:'M10 3a7 7 0 1 0 0 14 7 7 0 0 0 0-14m5 12 6 6', plus:'M12 4v16M4 12h16', settings:'M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8M12 2v3m0 14v3M2 12h3m14 0h3M5 5l2 2m10 10 2 2M5 19l2-2M17 7l2-2', arrow:'M5 12h14m-5-5 5 5-5 5', book:'M3 3h7l2 3 2-3h7v17h-7l-2 2-2-2H3ZM12 6v16', menu:'M4 6h16M4 12h16M4 18h16' };
export const icon = (name) => `<svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="${paths[name] || paths.notes}"/></svg>`;
export const button = (label, href, kind='secondary') => `<a class="button ${kind}" href="${escape(href)}">${label}</a>`;
export function sidebar(active) { return `<aside class="sidebar" id="navigation"><a class="brand" href="/">${icon('book')}<span>Folio</span><span class="edition">PERSONAL</span></a><button class="search-trigger" data-search>${icon('search')}<span>Search your notes</span><kbd>⌘ K</kbd></button><nav aria-label="Main navigation">${[['home','Home','/'],['planner','Planner','/planner'],['todo','Todo','/todo'],['notes','Notes','/notes']].map(([id,label,url])=>`<a class="nav-item ${id===active?'selected':''}" ${id===active?'aria-current="page"':''} href="${url}">${icon(id)}${label}</a>`).join('')}</nav><div class="notebook-label eyebrow">Folders</div><nav aria-label="Folders">${['Daily','Projects','Personal','Archive'].map(n=>`<a class="nav-item folder" href="/notes?notebook=${n}">${icon('folder')}${n}</a>`).join('')}</nav><div class="sidebar-bottom"><form method="post" action="/notes/new"><button class="button primary full">${icon('plus')} New note <kbd>⌘ N</kbd></button></form><button class="nav-item theme-toggle" data-theme-toggle aria-label="Switch to dark mode"><span aria-hidden="true">◐</span><span data-theme-label>Dark mode</span></button><a class="nav-item" href="/settings">${icon('settings')}Settings</a><span class="sidebar-caption">A little space for what matters.</span></div></aside>`; }
export const sectionHeading = (title, action='') => `<div class="section-heading"><h2>${title}</h2>${action}</div>`;
export function taskRow(task, returnTo='/todo') { return `<div class="task-row ${task.completed?'is-complete':''}"><form method="post" action="/tasks/${escape(task.id)}/toggle"><input type="hidden" name="returnTo" value="${escape(returnTo)}"><button class="task-check" aria-label="${task.completed?'Reopen':'Complete'} ${escape(task.title)}" aria-pressed="${task.completed}">${task.completed?'✓':''}</button></form><div class="task-copy"><span>${escape(task.title)}</span>${task.source?`<a class="task-source" href="/notes?id=${escape(task.source)}">From note ↗</a>`:''}</div><span class="meta">${escape(task.time || '')}</span><form class="task-date" action="/tasks/${escape(task.id)}/date" method="post"><input type="hidden" name="returnTo" value="${escape(returnTo)}"><input aria-label="Due date for ${escape(task.title)}" name="date" type="date" value="${escape(task.date||'')}"><button title="Save due date" aria-label="Save due date for ${escape(task.title)}">↵</button></form></div>`; }
export const taskForm = (date,returnTo) => `<form class="inline-add" method="post" action="/tasks"><span aria-hidden="true">＋</span><input aria-label="New task" name="title" placeholder="Add an intention…" required maxlength="240"><input type="hidden" name="date" value="${escape(date)}"><input type="hidden" name="returnTo" value="${escape(returnTo)}"><button>Add <span aria-hidden="true">↵</span></button></form>`;
export const noteRow = (note) => `<a class="note-row" href="/notes?id=${escape(note.id)}"><div><h3>${escape(note.title)}</h3><p>${escape(note.body.replace(/[#*>`\[\]]/g,'').slice(0,105))}</p><span class="meta">${escape(note.notebook)} <span class="dot-separator">·</span> ${escape(new Date(note.updated).toLocaleDateString('en-US',{month:'short',day:'numeric'}))}</span></div><span class="note-arrow" aria-hidden="true">↗</span></a>`;
export const goalCard = (goal,index) => `<article class="goal-card"><div class="eyebrow">Direction ${String(index+1).padStart(2,'0')}<span class="status-dot"></span></div><h3>${escape(goal.title)}</h3><p>${escape(goal.description)}</p><div class="goal-footer"><span class="meta">${escape(goal.status)}</span><form action="/goals/${escape(goal.id)}/toggle" method="post"><button class="text-button">${goal.status==='active'?'Complete ↗':'Reopen ↗'}</button></form></div></article>`;
export function layout({active='home',title='Home',content,notice=''}) { return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(title)} · Folio</title><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600&family=JetBrains+Mono:wght@400;500&family=Newsreader:ital,wght@0,400;0,500;1,400&display=swap" rel="stylesheet"><link rel="stylesheet" href="/tokens.css"><link rel="stylesheet" href="/app.css"><script src="/theme.js"></script><script src="/app.js" defer></script></head><body><a href="#main" class="skip-link">Skip to content</a>${sidebar(active)}<div class="workspace"><header class="topbar"><div class="breadcrumb"><button class="icon-button menu-button" aria-label="Toggle navigation" aria-expanded="false" aria-controls="navigation" data-menu>${icon('menu')}</button>${icon('book')}<span>Personal workspace</span><span>/</span><strong>${escape(title)}</strong></div><div class="topbar-right"><span class="save-stamp"><i></i>Local preview</span><button class="icon-button" aria-label="Search notes" data-search>${icon('search')}</button><span class="avatar" aria-label="Personal workspace">F</span></div></header>${notice?`<p class="notice" role="status">${escape(notice)}</p>`:''}<main id="main" class="${active==='notes'?'notes-main':'main'}">${content}</main></div><dialog id="search-dialog" aria-labelledby="search-title"><div class="dialog-heading"><h2 id="search-title">Find a thought.</h2><button class="icon-button" data-close aria-label="Close search">×</button></div><form action="/notes" method="get" class="search-form">${icon('search')}<input type="search" name="q" aria-label="Search notes" placeholder="Search titles and words…" autofocus><button class="button primary">Search</button></form><p class="meta">Search your notes by title or content. <kbd>Esc</kbd> to close.</p></dialog></body></html>`; }
