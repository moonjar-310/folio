# Folio 디자인 검수 상태 — 2026-09-14

상태: **디자인 정리 및 최종 저장 완료**

## Penpot 구성

- 파일: `Folio — Web Design System`
- `01 — Design System`: Foundations, 공통 Core, 상태별 variants, SVG 아이콘, Source patterns, 화면 템플릿, 프론트 구현 계약.
- `02 — Screens`: Home Light, Todo, Planner, Notes, Home Dark의 연결된 템플릿 인스턴스 5개. 이전 화면 8개는 숨김·잠금된 `Previous drafts — retained backup` 그룹으로 보존.
- `03 — Archive`: 원본 참고 이미지, 이전 토큰·컴포넌트 및 반응형 초안.
- 저장 버전: `Folio — project and design system rename`.

## 수정 및 검증

- 5개 화면을 PNG로 내보내 시각적으로 확인했다. 원본 Stitch 스크린샷과 HTML을 참고했다.
- Planner의 누락된 곡선 그래픽을 편집 가능한 SVG로 복원하고 아이콘 배경 투명도를 수정했다.
- 브랜드 엠블럼을 공통 컴포넌트로 연결했다.
- Todo의 미완료 항목에 잘못 표시되던 체크 9개를 숨겨 완료 상태와 구분했다.
- Notes 제목·미리보기 겹침·코드 배경을 수정하고, 완료 항목의 불필요한 기본 시각을 숨겼다. 읽기 정보는 두 줄로 정리했다.
- Home 카드의 설명 길이에 따라 높이가 늘어나도록 수정했다.
- Dark Home의 원본 주 제목은 작게 제공되었다. 라이트 화면과 일관되도록 44px 제목으로 조정했다.
- 폰트 ligature 아이콘을 SVG 공통 요소로 교체했다. SVG 52종과 라이선스를 로컬에도 제공한다.
- 공통 컴포넌트는 내용별 복제 대신 데이터·상태·슬롯으로 구현하도록 props 계약을 작성했다. 버튼 및 작업 상태 variants, 긴 텍스트와 좁은 카드 폭 사례를 포함한다.
- 최종 `currentFile.validate()` 결과: 오류 0개. Folio 라이브러리 항목 122개이며, 이 수에는 원본 패턴과 화면 템플릿도 포함된다. 프론트 컴포넌트 122개를 만들라는 의미가 아니다.

## 전달 범위

편집 가능한 Penpot 디자인, 공통 컴포넌트 및 구현 계약을 제공한다. 원본의 내용과 구성을 유지하면서 브랜드·가독성·컴포넌트 동작을 조정했으므로 픽셀 단위 복제본은 아니다. 동작하는 프론트 애플리케이션, API 연동 및 모든 화면의 반응형 구현은 이 디자인 전달물에 포함되지 않는다.
